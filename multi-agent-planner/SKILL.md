name: multi-agent-planner
description: Designs complete multi-agent AI workflows with a planner orchestrator and specialised worker agents — including system prompts, routing logic, tool assignments, and handoff protocols. Use whenever a user says "design a multi-agent system", "I need a planner and worker agents", "orchestrate multiple AI agents", "build an agentic pipeline", or "how do I coordinate multiple Claude agents".

# Multi-Agent Planner

Produces a fully scaffolded multi-agent workflow: planner orchestrator prompt, worker agent prompts, routing decision logic, and handoff protocol — ready to drop into Claude Code or any agentic framework.

**Input**: Description of the overall goal and the specialised tasks needed
**Output**: Planner prompt + worker agent prompts + routing logic + handoff protocol

## Trigger phrases
- "design multi-agent workflow"
- "planner and worker agents"
- "orchestrate agents"
- "agentic pipeline"
- "coordinate multiple Claude agents"
- "build agent swarm"

## Source
`SKILL-45.md`